spring.application.name=BillingSoftware

# Database configuration
# environment; the values after ':' are local-dev fallbacks only.
spring.datasource.url=jdbc:mysql://localhost:3306/billing_app
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Chauhan#333}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.servlet.context-path=/api/v1.0

# AWS S3
aws.access.key=${AWS_ACCESS_KEY:}
aws.secret.key=${AWS_SECRET_KEY:}
aws.region=${AWS_REGION:ap-south-1}
aws.bucket.name=${AWS_BUCKET_NAME:}

# JWT - must be at least 32 characters for HS256
jwt.secret.key=${JWT_SECRET_KEY:7V8mQ2xL9pR4kT6wN3zY5cH1sF8aD0eG6jK2mP9qW4uX7bC1vN5rS8tL3}

# Optional: seeds the first admin, only when the users table is empty.
app.bootstrap.admin.email=${BOOTSTRAP_ADMIN_EMAIL:anurag@gmail.com}
app.bootstrap.admin.password=${BOOTSTRAP_ADMIN_PASSWORD:123456}

razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_98LcvTzaWTkqqz}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:U3iRigXOREq6N7aUh3w6MZJF}
